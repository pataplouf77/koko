{% extends 'base.html.twig' %}

{% block title %}Hello VoitureController Patrick  !{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello tank ! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/wamp/www/MagicLink/src/Controller/VoitureController.php'|file_link(0) }}">src/Controller/VoitureController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/wamp/www/MagicLink/templates/voiture/index.html.twig'|file_link(0) }}">templates/voiture/index.html.twig</a></code></li>
    </ul>
</div>
<div class="example-wrapper">
    <h1>Hello Voiture list  ✅</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
              
              
            </tr>
        </thead>
        <tbody>
        {% for tank in tanks %}
            <tr>
                <td>{{ tank.id }}</td>
                <td>{{ tank.name }}</td>
             
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>

{% endblock %}
